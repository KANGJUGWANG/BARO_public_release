# Data Directory

This public repository does not include production database exports, raw crawler payloads, parquet files, or full training datasets.

Use `data/sample/` for tiny synthetic rows that explain schema shape only.
