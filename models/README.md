# Models Directory

Real model artifacts are not included in this public repository.

Expected private artifacts include metadata, thresholds, encoding mappings, feature columns, and model binary files. Provide them separately in a private environment when running full inference.

Do not commit model binaries to this directory.
